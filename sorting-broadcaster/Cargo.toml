[package]
name = "sorting-broadcaster"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.6", features = ["ws"] }
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
dashmap = "5"
dotenvy = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
futures-util = "0.3"
uuid = { version = "1", features = ["v4"] }
tower-http = { version = "0.4", features = ["cors"] }

